#ifndef DRAW_ALIGNMENTS_H
#define DRAW_ALIGNMENTS_H

enum class TextAlign { LEFT, CENTER, RIGHT };

enum class Horizontal { FREE, LEFT, CENTER, RIGHT };

enum class Vertical { FREE, TOP, CENTER, BOTTOM };

#endif  // DRAW_ALIGNMENTS_H