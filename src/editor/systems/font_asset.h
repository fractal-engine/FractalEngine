#ifndef FONT_ASSET_H
#define FONT_ASSET_H

/*
------------------ STUB ------------------
Should include the following:
- FontAsset::OnDefaultLoad: load font metadata from given path
- FontAsset::RenderInspectableUI: draw UI in editor to inspect a font; if font isn’t loaded, prompt user to load it
- FontAsset::Icon: return a font icon for use in editor UI
- FontAsset::Load: should load a font from disk (supports .ttf and .otf)

*/

#endif // FONT_ASSET_H